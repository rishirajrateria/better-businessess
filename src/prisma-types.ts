// Re-export Prisma model types for use in client components (types only, no runtime import).
export type { Post, Project, Testimonial, ClientLogo, Faq, Lead, PageView, Event, User } from "@prisma/client";
